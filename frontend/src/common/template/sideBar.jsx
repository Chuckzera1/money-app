import React from 'react'
import Menu from './menu'

export default props => (
    <asiede className="main-sidebar">
        <section className="sidebar">
            <Menu/>
        </section>
    </asiede>
)